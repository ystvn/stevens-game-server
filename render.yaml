services:
  - type: web
    name: stevens-games
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --bind 0.0.0.0:10000"
    envVars:
      - key: FIREBASE_CREDS
        fromSecret: FIREBASE_CREDS